export interface ButtonProps {
    title: string,
    link: string,
    className?: string,
    children?: React.ReactNode
}

export interface HeaderProps {
    title: string,
    description?: string,
    children?: React.ReactNode,
    className?: string
}